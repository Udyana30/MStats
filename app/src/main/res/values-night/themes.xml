<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MStats" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/gray</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorAccent">@color/light_gray</item>
        <!-- Secondary colors -->
        <item name="colorSecondary">@color/deep_orange</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- App bar and status bar -->
        <item name="android:statusBarColor">@color/dark_gray</item>
        <item name="android:navigationBarColor">@color/gray</item>
    </style>
</resources>